(async()=>{const root=document.querySelector('#fresh-grid');try{const r=await fetch(window.offerUrl);const items=await r.json();root.innerHTML=items.map(x=>{const state=x.na_voljo?(x.zaloga==='omejena'?'🟠 Omejena količina':'🟢 Na voljo'):'⚪ Trenutno ni na voljo';return `<article class="card offer"><div class="offer-icon">${x.slika||'🍄'}</div><div class="status">${state}</div><h3>${x.ime}</h3><em>${x.latinsko||''}</em><p>${x.opis||''}</p><p><strong>${x.cena} € / ${x.enota}</strong></p><p class="meta">Pakiranja: ${(x.pakiranja||[]).join(', ')}<br>Sezona: ${x.sezona||'—'}${x.datum_pobiranja?`<br>Pobrano: ${x.datum_pobiranja}`:''}</p>${x.na_voljo?`<a class="btn primary" href="${x.kontakt}">Povpraševanje</a>`:''}</article>`}).join('')||'<p>Trenutno ni objavljene ponudbe.</p>'}catch(e){root.innerHTML='<div class="notice">Ponudbe trenutno ni mogoče naložiti.</div>'}})();
